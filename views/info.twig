<div id="o-szkole" class="row clearfix">
    <div class="col-3">
        <div class="section-heading">
            <h3>{{ infoArr.getHeading3 | raw }}</h3>
            <h2 class="section-title">{{ infoArr.getHeading2 | raw }}</h2>
            <p class="section-subtitle">{{ infoArr.getShortDesc | raw }}</p>
        </div>
    </div>
    <div class="col-2">
        {{ infoArr.getLongDesc | raw }}
    </div>
</div>